import React from 'react';
import FullScreenMealTable from './ FullScreenMealTable.js';

const MealTable = ({ meals }) => {
  return <FullScreenMealTable meals={meals} />;
};

export default MealTable;
